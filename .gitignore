node_modules/
__pycache__/
*.pyc
.DS_Store
.claude/worktrees/
.worktrees/
.claude/agents/
.security-scan/
/cache/

.antigravitycli/

# Runtime state files (session-specific, not repo artifacts)
.harness-curator-state.json
.agents/.maintenance-stamp
skills-lock.json

# legacy skill-dev-orchestrator scratch — no longer generated (artifacts moved to
# the session scratchpad dir); directory may still contain dead leftovers, left in place
_workspace/

# delegation-gate evidence files (session-scoped, safe to delete)
.claude/tmp/

# hwpx skill temp working directory
.hwpx_work/

# repo-quiz local progress (personal, not shared)
.repo-quiz/

node_modules/
__pycache__/
*.pyc
.venv/
.DS_Store
.claude/worktrees/
.worktrees/
# harness role files ARE tracked (validate-harness.sh §11 checks them);
# only the local copy of the plugin-shipped prod:persona-actor stays ignored
.claude/agents/persona-actor.md
.security-scan/
/cache/

.antigravitycli/

# Runtime state files (session-specific, not repo artifacts)
.harness-curator-state.json
.agents/.maintenance-stamp
skills-lock.json

# delegation-gate evidence files (session-scoped, safe to delete)
.claude/tmp/

# hwpx skill temp working directory
.hwpx_work/

# repo-quiz local progress (personal, not shared)
.repo-quiz/

{
  "description": "SessionStart staleness nudge for the harness-curator skill. Tracks days since last harness analysis via ~/.claude/.task-audit-state.json; emits a one-line reminder when stale (>7d), throttled to once per 24h. Never blocks startup.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/task-audit-nudge/nudge.py\"",
            "timeout": 5,
            "statusMessage": "Checking harness-curator staleness..."
          }
        ]
      }
    ]
  }
}

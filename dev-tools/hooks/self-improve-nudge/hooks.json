{
  "description": "Stop hook: per-session learning-capture gate. Detects complex tasks, error-recovery, and user corrections; blocks Stop once (per session + 4h cross-session throttle) to nudge Claude to write lessons before context expires. Distinct from harness-curator (cross-session retrospective).",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT:-${PLUGIN_ROOT}}/hooks/self-improve-nudge/nudge.py\"",
            "timeout": 10,
            "statusMessage": "Checking session for capture signals..."
          }
        ]
      }
    ]
  }
}

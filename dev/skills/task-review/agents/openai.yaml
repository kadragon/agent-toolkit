interface:
  display_name: "Review Cycle"
  short_description: "Commit, collect reviews, apply findings, wait for CI, and merge"
  default_prompt: "Run the review cycle on this branch and merge once CI is green."
policy:
  allow_implicit_invocation: false

interface:
  display_name: "Harness Curate"
  short_description: "Mine session history to propose or prune harness assets"
  default_prompt: "Audit my harness against recent sessions and propose what to add or retire."
policy:
  allow_implicit_invocation: false

interface:
  display_name: "Harness Curate"
  short_description: "Mine session history to propose or prune harness assets"
  default_prompt: "Use $harness-curate to audit my harness against recent sessions and propose what to add or retire."
policy:
  allow_implicit_invocation: false

[
  {"query": "synthesize what we already know into a design doc under docs/design", "should_trigger": true},
  {"query": "this work is architecturally significant and spans multiple sessions — write the design doc first", "should_trigger": true},
  {"query": "produce a design document for this feature before we start coding", "should_trigger": true,
   "waived": "lexical proxy false negative, measured: only 5 of the 11 query tokens survive the corpus filter — a, before, design, for, this. `document` is out-of-corpus, so the one discriminating noun is dropped and four of the five survivors are function words the tokenizer has no stopword list to remove. task-tickets wins at 0.2560 over task-spec at 0.2013. A real trigger case — kept for skill-creator's model-judged runner."},

  {"query": "break the approved spec into vertical-slice backlog items in dependency order", "should_trigger": false},
  {"query": "interview me one question at a time until the scope is unambiguous", "should_trigger": false},
  {"query": "set up AGENTS.md and a docs index for this repo", "should_trigger": false}
]

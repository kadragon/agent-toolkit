{
  "description": "SessionStart staleness nudge for the harness-curate skill (folder keeps the task-audit name for migration continuity). Tracks each project's last harness analysis via <config-dir>/projects/<resolved-project>/.harness-curator-state.json; emits a one-line reminder when stale (>7d), throttled to once per 24h. Never blocks startup.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT:-${PLUGIN_ROOT}}/hooks/task-audit-nudge/nudge.py\"",
            "timeout": 5,
            "statusMessage": "Checking harness-curate staleness..."
          }
        ]
      }
    ]
  }
}
